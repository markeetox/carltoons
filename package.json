{
  "name": "tooniseum",
  "version": "1.0.0",
  "private": true,
  "description": "Pigeon Tamagotchi battle game — pure static site, no build step",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "firebase-admin": "^12.0.0"
  }
}
